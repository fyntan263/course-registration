import { CourseRegistrationUtils } from './course-registration-utils';

describe('CourseRegistrationUtils', () => {
  it('should create an instance', () => {
    expect(new CourseRegistrationUtils()).toBeTruthy();
  });
});
